# /bench

Run benchmarks for the current crate (or whole workspace).

```bash
cargo bench --workspace
```

Note: benchmarks are nightly-only in v1. Requires `cargo +nightly bench` or the `bench` feature on the relevant crate.
